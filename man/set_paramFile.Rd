% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/5_set_paramFile.R
\name{set_paramFile}
\alias{set_paramFile}
\title{Modify set_parameter_filenames.csv}
\usage{
set_paramFile(input = "input_data_latin_hypercube.csv")
}
\arguments{
\item{input}{Character. Name of the input data CSV file to be used by SCOPE.
Default is \code{"input_data_latin_hypercube.csv"}.
You can also specify \code{"input_data_default.csv"} to run individual simulations
or time series experiments.}
}
\value{
A character vector representing the rows of \code{set_parameter_filenames.csv}.
Writing to file is not done inside the function, allowing users to
manage file outputs explicitly (e.g. with \code{\link[=writeLines]{writeLines()}} or \code{\link[utils:write.table]{utils::write.table()}}).
}
\description{
This function generates the content for \code{set_parameter_filenames.csv}
and allows you to switch between different input data CSV files
used by SCOPE. By default, the parameter file points to
\code{"input_data_latin_hypercube.csv"}, but you can change it to
\code{"input_data_default.csv"} or any other input file that follows
the expected structure.
}
\examples{
# Default: points to latin hypercube input
set_paramFile()

# Switch to default input data file
set_paramFile(input = "input_data_default.csv")

# Write to file (outside the function)
# lines <- set_paramFile("input_data_default.csv")
# writeLines(lines, "./SCOPE/set_parameter_filenames.csv", useBytes = TRUE)

}
\seealso{
\code{\link[=set_options]{set_options()}}, \code{\link[=set_filenames]{set_filenames()}}, \code{\link[=input_data_default]{input_data_default()}}
}
