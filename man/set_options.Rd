% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/4_set_options.R
\name{set_options}
\alias{set_options}
\title{Generate or modify setoptions.csv content}
\usage{
set_options(
  verify = TRUE,
  simulation = 0,
  lite = TRUE,
  calc_fluor = TRUE,
  calc_planck = TRUE,
  calc_xanthophyllabs = TRUE,
  soilspectrum = FALSE,
  fluorescence_model = FALSE,
  applTcorr = TRUE,
  saveCSV = TRUE,
  mSCOPE = FALSE,
  calc_directional = FALSE,
  calc_vert_profiles = FALSE,
  soil_heat_method = 2,
  calc_rss_rbs = FALSE,
  MoninObukhov = TRUE,
  save_spectral = TRUE
)
}
\arguments{
\item{verify}{Logical. Enable verification step. Default: \code{TRUE}.}

\item{simulation}{Integer. Simulation flag (typically \code{0} or \code{1}). Default: \code{0}.}

\item{lite}{Logical. Use "lite" option. Default: \code{TRUE}.}

\item{calc_fluor}{Logical. Enable fluorescence calculations. Default: \code{TRUE}.}

\item{calc_planck}{Logical. Enable Planck law calculations. Default: \code{TRUE}.}

\item{calc_xanthophyllabs}{Logical. Enable xanthophyll absorption calculations. Default: \code{TRUE}.}

\item{soilspectrum}{Logical. Use soil spectrum. Default: \code{FALSE}.}

\item{fluorescence_model}{Logical. Enable fluorescence model. Default: \code{FALSE}.}

\item{applTcorr}{Logical. Apply temperature correction. Default: \code{TRUE}.}

\item{saveCSV}{Logical. Save results as CSV. Default: \code{TRUE}.}

\item{mSCOPE}{Logical. Use mSCOPE module. Default: \code{FALSE}.}

\item{calc_directional}{Logical. Enable directional calculations. Default: \code{FALSE}.}

\item{calc_vert_profiles}{Logical. Enable vertical profiles. Default: \code{FALSE}.}

\item{soil_heat_method}{Integer. Soil heat calculation method. Default: \code{2}.}

\item{calc_rss_rbs}{Logical. Enable RSS/RBS calculations. Default: \code{FALSE}.}

\item{MoninObukhov}{Logical. Enable Monin–Obukhov similarity theory. Default: \code{TRUE}.}

\item{save_spectral}{Logical. Save spectral outputs. Default: \code{TRUE}.}
}
\value{
A data frame with two columns:
\describe{
\item{flag}{Numeric flags (0, 1, or 2) indicating module settings.}
\item{module}{Character vector of module names.}
}
}
\description{
This function creates the template of \code{setoptions.csv} from scratch
and updates the numeric flags according to the user's arguments.
It returns the data frame so writing to disk can be handled outside
the function (e.g., with \code{\link[utils:write.table]{utils::write.table()}} or \code{\link[readr:write_delim]{readr::write_csv()}}).
}
\examples{
# Create default setoptions table
df <- set_options()
head(df)

# Modify specific options
df <- set_options(verify = FALSE, simulation = 1, soilspectrum = TRUE)

# Write to CSV
# utils::write.table(df, file = "setoptions.csv", sep = ",",
#                    col.names = FALSE, row.names = FALSE, quote = FALSE)

}
